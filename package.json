{
  "scripts": {
    "vercel-build": "chmod +x install-flutter.sh && ./install-flutter.sh && ./flutter/bin/flutter clean && ./flutter/bin/flutter pub get && ./flutter/bin/flutter build web --release"
  }
}
